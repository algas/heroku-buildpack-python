# Install dependencies with Pip.
puts-step "Installing dependencies with pip"

echo `ls $BUILD_DIR/.apt/usr/lib/x86_64-linux-gnu/pkgconfig`
echo `ls $BUILD_DIR/.heroku/vendor/lib/pkgconfig`
echo `ls /app/.heroku/vendor/lib/pkgconfig`
export PKG_CONFIG_PATH=$PKG_CONFIG_PATH:$BUILD_DIR/.apt/usr/lib/x86_64-linux-gnu/pkgconfig
echo $PKG_CONFIG_PATH

[ ! "$FRESH_PYTHON" ] && bpwatch start pip_install
[ "$FRESH_PYTHON" ] && bpwatch start pip_install_first

/app/.heroku/python/bin/pip install -r requirements.txt --exists-action=w --src=./.heroku/src --allow-all-external  | cleanup | indent

# Smart Requirements handling
cp requirements.txt .heroku/python/requirements-declared.txt
/app/.heroku/python/bin/pip freeze > .heroku/python/requirements-installed.txt

[ ! "$FRESH_PYTHON" ] && bpwatch stop pip_install
[ "$FRESH_PYTHON" ] && bpwatch stop pip_install_first

echo
